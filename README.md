# 🚀 Туториал: Деплой статического сайта на Firebase с автосинхронизацией через GitHub

## Что получим в итоге
Каждый раз когда ты вносишь изменения и делаешь `git push` — сайт автоматически обновляется. Бесплатно.

---

## Что нужно установить заранее

- [Node.js](https://nodejs.org/) (LTS версия)
- [Git](https://git-scm.com/)
- Аккаунт на [GitHub](https://github.com/)
- Аккаунт на [Firebase](https://firebase.google.com/) (через Google)

---

## Шаг 1 — Создать репозиторий на GitHub

1. Зайди на [github.com](https://github.com/) → **New repository**
2. Дай имя (например `my-site`)
3. Оставь **Public**, нажми **Create repository**

---

## Шаг 2 — Создать проект в Firebase

1. Зайди на [console.firebase.google.com](https://console.firebase.google.com/)
2. Нажми **Add project** → дай имя → отключи Google Analytics (необязательно) → **Create project**
3. В левом меню выбери **Hosting** → **Get started** → следуй инструкции

---

## Шаг 3 — Установить Firebase CLI

Открой терминал (PowerShell на Windows) и выполни:

```bash
npm install -g firebase-tools
```

---

## Шаг 4 — Войти в Firebase

```bash
firebase login
```

Откроется браузер — войди через Google аккаунт.

---

## Шаг 5 — Подготовить папку с сайтом

Структура папки должна быть такой:
```
моя-папка/
└── public/
    └── index.html   ← твой главный HTML файл
```

Открой терминал **в папке проекта** и выполни:

```bash
firebase init hosting
```

Отвечай на вопросы так:

| Вопрос | Ответ |
|--------|-------|
| Which project? | выбери свой проект из списка |
| Public directory? | `public` + Enter |
| Single-page app? | `n` |
| Overwrite index.html? | `n` |

---

## Шаг 6 — Подключить GitHub Actions

Выполни:

```bash
firebase init hosting:github
```

Отвечай на вопросы так:

| Вопрос | Ответ |
|--------|-------|
| GitHub repository? | `user/repository` (например `user110611/my-site`) |
| Build script before deploy? | `n` |
| Overwrite PR workflow file? | `n` |
| Auto deploy on merge? | `y` |
| Branch for live channel? | Enter (оставь `main`) |
| Overwrite merge workflow? | `y` |
| Overwrite index.html? | `n` |

---

## Шаг 7 — Инициализировать Git и запушить

```bash
git init
git remote add origin https://github.com/ТВОЙюзер/ТВОЙрепозиторий.git
git add .
git commit -m "first commit"
git branch -M main
git push -u origin main
```

---

## Шаг 8 — Проверить деплой

1. Зайди на GitHub → твой репозиторий → вкладка **Actions**
2. Убедись что workflow запустился и завершился с зелёной галочкой ✅
3. Открой сайт по адресу: `https://ИМЯ-ПРОЕКТА.web.app`

---

## Как обновлять сайт в будущем

Внёс изменения в файлы → открой терминал в папке проекта → выполни:

```bash
git add .
git commit -m "описание изменений"
git push
```

Через ~1 минуту сайт обновится автоматически.

---

## Полезные команды

```bash
firebase deploy          # задеплоить вручную без GitHub
firebase hosting:channel:deploy preview   # задеплоить preview версию
git status               # посмотреть какие файлы изменились
git log --oneline        # история коммитов
```

---

## Структура файлов после настройки

```
проект/
├── .github/
│   └── workflows/
│       ├── firebase-hosting-merge.yml        ← автодеплой при push
│       └── firebase-hosting-pull-request.yml ← preview для PR
├── public/
│   ├── index.html   ← главная страница
│   └── 404.html     ← страница ошибки
├── .firebaserc      ← настройки Firebase проекта
├── .gitignore
└── firebase.json    ← настройки хостинга
```

---

*Туториал составлен для Windows (PowerShell). На Mac/Linux команды те же.*
